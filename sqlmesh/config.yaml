
gateways:
  motherduck:
    connection:
      type: motherduck
      database: ${{ MOTHERDUCK_DATABASE }}
      token: ${{ MOTHERDUCK_TOKEN }}
    state_connection:
      type: duckdb
      database: motherduck_state.db

default_gateway: motherduck

model_defaults:
  dialect: duckdb
  start: 2024-11-16


